Bootstrap: docker
From: mambaorg/micromamba:1.5.8

%files
    environments/environment.yml /tmp/environment.yml

%post
    micromamba install -y -n base -f /tmp/environment.yml
    micromamba clean -a -y

%runscript
    exec "$@"
