# Stage 4

Stage 4 consumes Stage 3 AA coordinates/DMS, nonprotein resources, `inputs/aa_protlig.pdb`, and configured ligand parameters.

Stage 4 owns `gmx pdb2gmx` protein-chain ITP generation and final AA topology assembly. Rosetta and PyRosetta plugins are optional and disabled in the generic workflow.
